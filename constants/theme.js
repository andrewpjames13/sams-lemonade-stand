const theme = {
  colors: {
    primary: '#d5c73f',
    secondary: '#002300',
  },
  bold: 900,
  light: 300,
  spacing: (multiplier) => `${8 * multiplier}px`,
  borderRadius: '8px',
}

export default theme;
